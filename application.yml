# eureka:
#   instance:
#     hostname: registry
#     prefer-ip-address: true
#   client:
#     service-url:
#       defaultZone: http://user:${REGISTRY_SERVER_PASSWORD:password}@registry:8761/eureka/

# spring:
#   security:
#     user:
#       name: user
#       password: password

ribbon:
  ReadTimeout: 5000
  ConnectTimeout: 5000
  eureka:
    enabled: true
